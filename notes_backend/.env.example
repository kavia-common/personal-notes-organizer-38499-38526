# Optional environment variables for Personal Notes API
# Safe defaults are used when not provided. Do NOT commit a real secret to VCS.

# Secret used to salt password hashing and token generation (prototype)
SECRET_KEY=change-me-in-production

# Access token time-to-live in seconds (default: 86400 = 24h)
ACCESS_TOKEN_TTL_SECONDS=86400
